# With crawler

- `nutch.Dockerfile` - Dockerfile for building Nutch image
- `docker-compose.yml` - Docker Compose file for running Nutch and Solr integration in the multi-container env
-

# Without crawler (models only)

- `Dockerfile` - Dockerfile for recreating environment to train and evaluate models (recommended)
- `MPS.Dockerfile` - Dockerfile for recreating environment to train and evaluate models on Mac with Metal GPU (experimental)
